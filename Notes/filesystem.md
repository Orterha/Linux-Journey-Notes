# Linux Filse system Basics

## So far what I've learned so far
Linux uses a single root file system starting at the root "/" and spreads out into other directories like branches.
When opening the terminal you will usually start in your home directory "/home", everything is treated like a file or directory.
some other important directories:


## Commands practiced to navigate around the terminal:

pwd = (print working directory) self explanatory 
Ls = lists the files and directories in your current working directory


